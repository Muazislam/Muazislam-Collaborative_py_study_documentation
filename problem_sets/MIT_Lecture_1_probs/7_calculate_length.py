# Calculate Length: Ask the user to input a phrase and use the built-in len() function to print the number of characters in the input

phrase = input("Please enter a phrase....\n ")
print(len(phrase))
